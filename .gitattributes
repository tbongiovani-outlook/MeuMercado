# Normaliza quebras de linha automaticamente
* text=auto

# Scripts de shell PRECISAM usar LF (executados no macOS/Linux)
*.sh text eol=lf

# Scripts do Windows usam CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
